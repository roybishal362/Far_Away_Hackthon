# Kakehashi backend — lean runtime deps (verified on Python 3.14, Streamlit Cloud-safe).

# API
fastapi>=0.110
uvicorn[standard]>=0.27

# LLM brain (Groq — free, fast, OpenAI-compatible)
groq>=0.11

# Real-data tools
requests>=2.31
beautifulsoup4>=4.12   # parse official SSW/MOFA pages

# RAG grounding (pure-python keyword retrieval — reliable on any host)
rank-bm25>=0.2.2

# Security
cryptography>=42.0     # Fernet/AES encryption-at-rest

# Config
python-dotenv>=1.0     # load .env for local dev

# Product features
reportlab>=4.0         # Migration Dossier PDF export
pypdf>=4.0             # resume (PDF) parsing
python-multipart>=0.0.9  # file uploads (resume)
