# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env & secrets — NEVER commit API keys
.env
.streamlit/secrets.toml

# Data & artifacts (keep the repo light; track in README how to get data)
data/
data/raw/
*.ckpt
*.pt
*.pkl
*.joblib

# OS / editor
.DS_Store
.vscode/
.idea/

# Local push helper (no secret inside, but keep it out of the repo)
push-to-github.ps1

# Internal R2/R3 strategy — keep out of the public submission repo
ROADMAP_R2_R3.md
