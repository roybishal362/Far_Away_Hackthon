# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Env & secrets — NEVER commit API keys
.env
.streamlit/secrets.toml

# Data — ignore local saved plans + raw, but COMMIT data/fixtures/ (recorded demo fallback)
data/plans/
data/raw/
*.ckpt
*.pt
*.pkl
*.joblib

# Test cache
.pytest_cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Local push helper (no secret inside, but keep it out of the repo)
push-to-github.ps1

# Internal R2/R3 strategy — keep out of the public submission repo
ROADMAP_R2_R3.md
